<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="AndroidSDKInstaller.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Android SDK Platform Tools Installer" Height="600" Width="800"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Background" Value="#3DDC84"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="Android SDK Platform Tools Installer" 
                       FontSize="24" FontWeight="Light" Margin="0,0,0,20"/>
            <TextBlock Text="Select installation options:" 
                       FontSize="16" Margin="0,0,0,10"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="0,20">
            <GroupBox Header="Installation Location" Margin="0,0,0,20" Padding="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox x:Name="InstallPathTextBox" 
                             Padding="5"
                             Text="{Binding InstallationPath}"/>
                    <Button Content="Browse" 
                            Grid.Column="1" 
                            Margin="10,0,0,0"
                            Click="BrowseButton_Click"/>
                </Grid>
            </GroupBox>

            <CheckBox x:Name="AddToPathCheckBox" 
                      Content="Add to System PATH" 
                      IsChecked="True"
                      Margin="0,0,0,10"/>
            
            <CheckBox x:Name="CreateShortcutCheckBox" 
                      Content="Create Desktop Shortcut" 
                      IsChecked="True"
                      Margin="0,0,0,20"/>

            <GroupBox Header="Installation Progress" Padding="10">
                <StackPanel>
                    <TextBlock x:Name="StatusText" Text="Ready to install..." Margin="0,0,0,10"/>
                    <ProgressBar x:Name="InstallProgress" 
                                Height="20" 
                                Margin="0,0,0,10"/>
                    <TextBlock x:Name="ProgressText" 
                              Text="0%" 
                              HorizontalAlignment="Center"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>

        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button x:Name="CheckUpdatesButton" 
                    Content="Check for Updates" 
                    Click="CheckUpdatesButton_Click"
                    Margin="0,0,10,0"/>
            <Button x:Name="InstallButton" 
                    Content="Install" 
                    Click="InstallButton_Click"/>
        </StackPanel>
    </Grid>
</Window>